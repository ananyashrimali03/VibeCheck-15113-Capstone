# VibeCheck — Project Dependencies Reference
#
# This is a Node.js / Next.js project.
# Python is NOT used. This file is a human-readable reference only.
# The real dependency file is: package.json
#
# To install all dependencies, run:
#   npm install
#
# To start the dev server, run:
#   npm run dev
#
# Runtime dependencies (package.json > dependencies):
#   next@16.2.4              — React framework (App Router)
#   react@19.2.4             — UI library
#   react-dom@19.2.4         — React DOM renderer
#   @google/generative-ai    — Gemini AI SDK (added in feature branch)
#
# Dev dependencies (package.json > devDependencies):
#   typescript@^5            — Type checking
#   tailwindcss@^4           — Utility-first CSS
#   eslint@^9                — Linting
#
# External APIs used (no installation needed):
#   Google Gemini            — gemini-1.5-flash, requires GEMINI_API_KEY in .env.local
#   iTunes Search API        — Free, no key, https://itunes.apple.com/search
